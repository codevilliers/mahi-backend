secrets:
  secretKey: # django secret key
services:
  database:
    host: # database host
    user: # database user
    password: # database password
    name: # database name
    port: # database port, default: 5432
